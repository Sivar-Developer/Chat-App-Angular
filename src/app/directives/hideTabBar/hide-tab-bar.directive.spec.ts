import { HideTabBarDirective } from './hide-tab-bar.directive';

describe('HideTabBarDirective', () => {
  it('should create an instance', () => {
    const directive = new HideTabBarDirective();
    expect(directive).toBeTruthy();
  });
});
